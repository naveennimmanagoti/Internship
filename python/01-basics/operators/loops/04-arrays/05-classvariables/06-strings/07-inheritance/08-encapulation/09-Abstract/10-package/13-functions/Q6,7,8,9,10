'''def process(value):
    if isinstance(value, (int, float)):
        print("Square:", value * value)
    elif isinstance(value, str):
        print("Uppercase:", value.upper())


process(5)
process("python")'''
#program for advancd function
'''def total_price(price, discount=0, *items, **extra):
    total = price + sum(items)

    total = total - (total * discount / 100)

    print("Total:", total)
    print("Extra:", extra)


total_price(1000, 10, 200, 300, tax=50)'''
#program for lambda function
'''add = lambda a, b: a + b
square = lambda n: n * n

print("Addition:", add(10, 20))
print("Square:", square(5))'''
#program for ma function
'''numbers = [1, 2, 3, 4, 5]

result = list(map(lambda x: x * x, numbers))

print("Squares:", result)'''
#program for filter function
numbers = [1, 2, 3, 4, 5, 6]

result = list(filter(lambda x: x % 2 == 0, numbers))

print("Even numbers:", result)