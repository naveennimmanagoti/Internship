Q1
# Python Classes and Modules

Created two Python modules:

* `class_one.py` – Contains `ClassOne`
* `class_two.py` – Contains `ClassTwo`

Each class includes:

* A constructor `__init__()`
* An instance variable
* A method to display output

Concepts: Classes, Constructors, Methods, Variables, and Modules.
q
Q2
# Python Package

Created a package named `mypackage`.

* Moved `class_one.py` and `class_two.py` into `mypackage`.
* Created `__init__.py`.
* Imported both classes inside `__init__.py` for easy access.

Concepts: Python Packages, `__init__.py`, and Imports.
Q3
# Import Classes

Imported classes from `mypackage` using different import methods.

Concepts: Module Import and Class Import.
Q4
# Use the Classes

Created objects of both classes and called their methods to display output.

Concepts: Objects, Methods, and Class Usage.
Q5
# Final Program

Created a complete program using both classes from the package. Demonstrated constructors, objects, and methods with meaningful output.

Concepts: Packages, Constructors, Objects, and Methods.
Q6
# Alias Imports

Imported a module using an alias `c1` and used the class from the module.

concepts:Module Aliases and Imports.
Q7
# Relative Imports

Imported one class into another class using a relative import inside the package.

Concepts: Relative Imports and Packages.
Q8
# Real-World Structure

Organized the project using a `main.py` file and a `mypackage` folder containing the Python modules and `__init__.py`.

Structure:
`project/main.py → mypackage/__init__.py, class_one.py, class_two.py`

Concepts:Project Structure, Packages, and Modules.

